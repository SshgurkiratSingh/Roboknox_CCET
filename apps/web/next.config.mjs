/** @type {import('next').NextConfig} */
const nextConfig = {
    transpilePackages: ['three', '@react-three/postprocessing', '@react-three/drei', '@react-three/fiber']
};

export default nextConfig;
